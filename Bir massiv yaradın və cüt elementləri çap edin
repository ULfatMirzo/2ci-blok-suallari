using System;

class Program
{
    static void Main()
    {
        int[] arr = { 10, 15, 22, 33, 44, 55, 60 };

        Console.WriteLine("Massivdəki cüt ədədlər:");
        foreach (int num in arr)
        {
            if (num % 2 == 0)
                Console.WriteLine(num);
        }
    }
}
